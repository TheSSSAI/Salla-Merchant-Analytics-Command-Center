[{'name': 'API Gateway', 'type': 'Gateway', 'implementation': 'A managed service (e.g., AWS API Gateway) serves as the single entry point, routing HTTP requests to the appropriate backend serverless functions (e.g., AWS Lambda).', 'applicability': "Acts as the single, secure entry point for the frontend Single Page Application (SPA). It is essential for centralizing concerns like request routing, JWT-based authentication, Role-Based Access Control (RBAC), and rate limiting, as defined in the architecture's API Gateway Layer."}, {'name': 'Request-Reply', 'type': 'Synchronous Communication', 'implementation': 'Synchronous HTTP/REST calls initiated by the SPA to the API Gateway, which then invokes a serverless function and returns the response. The client blocks and waits for the response.', 'applicability': 'The primary pattern for all interactive user-facing features where an immediate response is required. This includes fetching analytics reports (REQ-FUNC-009), managing team members (REQ-FUNC-007), and submitting natural language queries to the AI Assistant (REQ-FUNC-014).'}, {'name': 'Message Queue', 'type': 'Asynchronous Messaging', 'implementation': 'An initial serverless function receives a request and places a message onto a durable queue (e.g., AWS SQS). A separate, decoupled worker function is triggered by the queue to perform the actual processing.', 'applicability': 'Directly required by REQ-PERF-002 to process complex, data-intensive operations asynchronously without blocking the user interface. This is essential for tasks like large report generation or data exports, ensuring the synchronous API call returns immediately.'}, {'name': 'Change Data Capture (CDC)', 'type': 'Data Integration', 'implementation': "The 'Data Ingestion Pipeline' service reads the transaction log of the PostgreSQL (OLTP) database to capture data changes (inserts, updates, deletes) as events. These events are then processed and streamed to the ClickHouse (OLAP) data warehouse.", 'applicability': 'This is the mandatory pattern specified in REQ-DATA-002 for synchronizing data between the OLTP and OLAP databases. It is a critical foundation for ensuring that all analytical reports, forecasts, and AI insights are based on up-to-date data.'}, {'name': 'Scheduler', 'type': 'Event-Driven Trigger', 'implementation': "A time-based trigger service (e.g., AWS EventBridge Scheduler) that invokes serverless functions from the 'Scheduled Tasks Service' at predefined intervals (e.g., cron expressions).", 'applicability': 'Essential for automating periodic background tasks. This pattern is required for purging old abandoned cart data (REQ-DATA-001), expiring temporary tokens for password resets and invitations (REQ-FUNC-003, REQ-FUNC-006), and proactively generating AI insight cards (REQ-FUNC-015).'}, {'name': 'Circuit Breaker', 'type': 'Reliability', 'implementation': "Implemented as a stateful wrapper around the OpenAI API client within the Infrastructure Layer. It monitors for failures, and if the failure rate exceeds a threshold, it 'trips' and fails subsequent calls fast, preventing further requests for a cool-down period. Includes a retry mechanism for transient faults.", 'applicability': 'A critical reliability pattern for the integration with the external OpenAI API (REQ-INTG-004). It prevents cascading failures within the AI Assistant service when the third-party API is slow or unavailable, ensuring the system remains stable and responsive.'}]

